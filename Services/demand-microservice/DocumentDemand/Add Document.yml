info:
  name: Add Document
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8087/demands
  body:
    type: json
    data: |-
      {
        "userId": 1,
        "documentType": "BIRTH_CERTIFICATE",
        "description": "Request for administrative purposes"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
